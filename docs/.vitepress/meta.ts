/**
 * @file meta.ts
 */

/**
 * npm package name, it's unique
 */
export const packageName = 'svg-eslint-parser'

/**
 * Shared meta info
 */
export const appTitle = packageName
export const appUrl = `https://${packageName}.ntnyq.com`
export const appDescription = 'An SVG parser that produces output compatible with ESLint.'
